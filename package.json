{
  "name": "rubicon-bathusdt",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ platocrat/rubicon-bathusdt",
    "create-local": "graph create --node http://localhost:8020/ rubicon-bathusdt",
    "remove-local": "graph remove --node http://localhost:8020/ rubicon-bathusdt",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 rubicon-bathusdt"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.22.2",
    "@graphprotocol/graph-ts": "^0.22.1"
  }
}